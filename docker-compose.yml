version: "3.8"

services:
  frontend-user:
    build:
      context: ./frontend-user
      dockerfile: Dockerfile
    container_name: frontend-user-container
    ports:
      - "8081:80"
    restart: always
